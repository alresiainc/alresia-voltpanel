# Build
/dist/
/bin/
/tmp/
# Generated UI bundle embedded by cmd/voltpanel (content-hashed filenames churn
# on every build); a placeholder cmd/voltpanel/dist/index.html stays tracked so
# `go build` works before `make ui-build` has ever run.
cmd/voltpanel/dist/assets/

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
coverage.out

# Node
ui/node_modules/
ui/dist/
ui/.turbo/
ui/.vite/

# OS / Editors
.DS_Store
.idea/
.vscode/
*.swp

.hide
.claude/worktrees/
start